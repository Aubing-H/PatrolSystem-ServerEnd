<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bingo.mapper.PatrolRecordMapper">

    <insert id="addPatrolRecord" parameterType="PatrolRecord">
        insert into `patrol_record` (`user_id`, `patrol_loc_id`, `time`, `condition`)
        values (#{userId}, #{patrolLocId}, #{time}, #{condition});
    </insert>

    <select id="queryOnePatrolRecord" resultType="PatrolRecord">
        select p.user_id, p.patrol_loc_id, p.time, p.condition
        from patrol_record as p, `user` as u
        where p.user_id = u.id and u.name = #{username} and
              p.patrol_loc_id = #{locationId} and p.time = #{time}
    </select>

    <select id="queryByInfo" resultType="PatrolRecord">
        select * from patrol_record
        where 1=1
        <if test="start != null and end != null">
            and `time` between #{start} and #{end}
        </if>
        <if test="userId != null">
            and user_id = #{userId}
        </if>
        <if test="locId != null">
            and patrol_loc_id = #{locId}
        </if> ;
    </select>

</mapper>